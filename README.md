# swatt-lasagna-ui

## Prerequisites

- [Node.js v22.20.0](https://nodejs.org/)

---

## Setup Instructions

1. **Initialize:**

   ```bash
   nvm install && nvm use && npm install && cp .env.example .env
   ```

2. **Run:**

   ```bash
   npm run dev
   ```
